<% if notice %>
  <div data-controller="alert" class="fixed bottom-4 left-1/2 -translate-x-1/2 z-50 flex items-center justify-between gap-4 rounded-lg bg-green-50 border border-green-200 px-4 py-3 text-green-800 shadow-lg">
    <div class="flex items-center gap-3">
      <svg class="h-5 w-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <p class="text-sm font-medium"><%= notice %></p>
    </div>
    <button type="button" data-action="click->alert#close" class="text-green-600 hover:text-green-800 transition-colors">
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>
<% end %>
<% if alert %>
  <div data-controller="alert" class="fixed bottom-4 left-1/2 -translate-x-1/2 z-50 flex items-center justify-between gap-4 rounded-lg bg-red-50 border border-red-200 px-4 py-3 text-red-800 shadow-lg">
    <div class="flex items-center gap-3">
      <svg class="h-5 w-5 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <p class="text-sm font-medium"><%= alert %></p>
    </div>
    <button type="button" data-action="click->alert#close" class="text-red-600 hover:text-red-800 transition-colors">
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>
<% end %>
